import { colors } from '@theme';
import { createUseStyles } from 'react-jss';

export default createUseStyles({
  root: {
    color: colors.WHITE,
    textAlign: 'left',
  },

  lineText: {
    marginLeft: 8,
    width: '100%',
  },

  currentText: {
    display: 'flex',
  },

  welcome: {},
  preWhiteSpace: {},
});
